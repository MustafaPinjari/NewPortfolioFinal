// Simple font configuration to avoid build issues
export const mukta = {
  className: 'font-sans',
  variable: '--font-sans',
};

export const copse = mukta;
export const dmMono = mukta;
export const merryWeather = mukta;
export const inter = mukta;
